export enum ReservationStatusTypes {
  pending = 'pending',
  rejected = 'rejected',
  accepted = 'accepted',
  overdue = 'overdue',
  returned = 'returned',
  cancelled = 'cancelled',
  checkedOut = 'checkedOut',
}
